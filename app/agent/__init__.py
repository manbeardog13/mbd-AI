"""The agent: Nero's ability to take a verified action and build on the result.

`state` is Executive Memory (the working-state register, ADR-0008); `loop` is the
reason → choose capability → execute → observe cycle (ADR-0003) that turns a goal
into a sequence of gated tool calls and a final answer.
"""
