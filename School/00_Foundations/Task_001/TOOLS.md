# Allowed and useful capabilities

- filesystem read
- repository search
- no plugin required

Availability is checked at execution time. Missing optional capabilities are reported; they are never invented or installed automatically.
