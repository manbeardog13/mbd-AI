# Task context

The request is to inspect a project, propose a reversible change, run targeted checks, and stop before publishing. Publishing, credential access, and deleting user files are outside scope.
