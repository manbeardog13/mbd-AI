# Task context

Available: rg for local text search, GitHub connector for authenticated repository metadata, Browser for public pages, Computer Use for UI-only fallbacks, and filesystem tools. MCP availability must be checked before use.
