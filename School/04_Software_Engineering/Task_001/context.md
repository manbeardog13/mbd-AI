# Task context

parse_port must accept integer strings from 1 through 65535, trim surrounding whitespace, and reject booleans, non-numeric strings, zero, negatives, and values above 65535 with ValueError.
